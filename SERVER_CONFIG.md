# 🔐 服务器配置指南

本文档说明如何在服务器上安全地配置API密钥和默认设置。

## 📝 配置方式

### 方法一：使用环境变量（推荐）

在服务器项目目录下创建 `.env` 文件：

```bash
cd /home/ubuntu/paper-summerizer

# 创建.env文件
cat > .env << 'EOF'
API_KEY=你的API密钥
BASE_URL=你的API基础URL
MODEL=gemini-2.0-flash-exp
EOF

# 设置文件权限（仅自己可读）
chmod 600 .env
```

### 方法二：直接在Web界面配置

首次访问时在Web界面中填入：
- API提供商：选择 Gemini
- API密钥：填入你的密钥
- API基础URL：填入你的API地址
- 勾选"保存配置"

配置会保存到容器内的 `config.json` 文件。

## 🔄 重启应用使配置生效

如果使用了 `.env` 文件：

```bash
docker compose down
docker compose up -d
```

## 🔒 安全提示

1. ✅ **永远不要**将包含真实密钥的 `.env` 文件提交到Git
2. ✅ `.env` 文件已在 `.gitignore` 中被排除
3. ✅ 使用 `chmod 600 .env` 限制文件权限
4. ✅ 定期更换API密钥

## 📋 配置说明

- `API_KEY`: 你的API密钥
- `BASE_URL`: API基础URL（例如：`http://your-server:3000/v1`）
- `MODEL`: 模型名称（默认：`gemini-2.0-flash-exp`）

这些环境变量会作为Web界面的**默认值**，你仍可以在界面中修改。
